name: genre-game-mlproject

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "data_preprocessing/videos_preprocessed.csv"}
    command: "python modelling.py --data_path {data_path}"
